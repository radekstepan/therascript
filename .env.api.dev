# File: .env.api.dev
# ==========================================================
# DEVELOPMENT Environment for Therascript API
# ==========================================================

# === General Server Configuration ===
PORT=3001
NODE_ENV=development
APP_MODE=development # <= Use real services
CORS_ORIGIN=http://localhost:3002

# --- Ollama Configuration ---
OLLAMA_BASE_URL=http://localhost:11434
# Use a potentially smaller/faster model for development
OLLAMA_MODEL=gemma3:1b-it-qat # Or gemma:2b, phi3:mini, etc.
OLLAMA_CHAT_KEEP_ALIVE=5m

# --- Whisper Configuration ---
WHISPER_API_URL=http://localhost:8000
# Use a faster model for development
WHISPER_MODEL=tiny # Or 'tiny'

# --- Database & File Storage Configuration ---
# (Relative to packages/api)
DB_PATH=./data/therapy-analyzer-dev.sqlite # Use a separate dev DB
DB_TRANSCRIPTS_DIR=./data/transcripts
DB_UPLOADS_DIR=./data/uploads

# --- Upload Configuration ---
UPLOAD_MAX_FILE_SIZE=1g

# --- Mock Mode Specific Configuration (Ignored when APP_MODE != mock) ---
# MOCK_WHISPER_DELAY_MS=500
# MOCK_OLLAMA_DELAY_MS=800
# MOCK_LLM_MODEL_NAME=mock-llama3:latest
