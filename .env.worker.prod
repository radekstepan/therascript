# PRODUCTION Environment for Therascript Worker

# === General Configuration ===
NODE_ENV=production
APP_MODE=production

# --- Redis Configuration ---
# Host and port for the Redis instance used by BullMQ
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Database & File Storage Configuration ---
# Paths are RELATIVE to the monorepo root directory.
# This MUST match the database used by the production API.
DB_PATH=packages/api/data/therapy-analyzer-prod.sqlite
DB_UPLOADS_DIR=packages/api/data/uploads

# --- Service URLs ---
# These must be accessible from the worker process.
WHISPER_API_URL=http://localhost:8000
WHISPER_MODEL=medium.en
OLLAMA_BASE_URL=http://localhost:11434
ELASTICSEARCH_URL=http://localhost:9200
