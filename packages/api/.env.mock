# This file should have the highest priority for APP_MODE
APP_MODE=mock
NODE_ENV=development # Usually run mock mode in a dev-like environment

# URLs below are typically ignored in mock mode, but good practice to set placeholders
OLLAMA_BASE_URL=http://mock-ollama-does-not-exist:11434
WHISPER_API_URL=http://mock-whisper-does-not-exist:8000

# Optional: Control mock behavior
MOCK_WHISPER_DELAY_MS=500
MOCK_OLLAMA_DELAY_MS=800
MOCK_LLM_MODEL_NAME=mock-llama3:latest
